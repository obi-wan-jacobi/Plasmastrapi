require.config({
    paths: {
        // Base Objects
        'loader': './utils/Base/Loader',
        // Loaders
        'image-loader': './utils/Loaders/ImageLoader',
        'sprite-loader': './utils/Loaders/SpriteLoader'
    }
});