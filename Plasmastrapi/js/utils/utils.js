define(['configurator', 'modules', 'validator'], function (configurator, modules, validator) {

    var utils = {
        configurator,
        modules,
        validator
    };

    return utils;
});